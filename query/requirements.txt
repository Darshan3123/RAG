# ── query/requirements.txt ───────────────────────────────
# GPU server (same machine as indexer, or separate).
# Install: pip install -r requirements.txt

# Core
python-dotenv==1.0.1

# Embedding model (must be the SAME model as indexer)
sentence-transformers==3.0.1
torch==2.3.0          # or torch==2.3.0+cu118 for CUDA GPU

# Vector store (read-only queries)
chromadb>=0.5.4

# Sparse retrieval
rank-bm25==0.2.2

# LLM — pick one or both
openai==1.35.0
requests==2.32.3      # for Ollama HTTP API
